import TextField from "@mui/material/TextField";

export const SearchBox = () => {
  return (
    <TextField
      id="outlined-basic"
      label="Search"
      variant="outlined"
      style={{ display: "flex", margin: 20 }}
    />
  );
};
